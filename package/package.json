{
  "name": "nextjs-generate",
  "version": "0.0.1-alpha.2",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup src/index.ts --minify --dts --format esm --out-dir dist"
  },
  "bin": {
    "next-generate": "./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  }
}
